package com.csiris.postservice.mapper

import com.csiris.postservice.persistence.Post;
import com.csiris.postservice.transport.PostDto;

/**
	 * A Kotlin extension for mapping Post entity to Post Dto Object and vice versa
	 * 
	 */
class DtoEntityMapperKotlinExtension {

//	fun PostDto.fromPostEntity(post: Post) =
//		PostDto(post.title, post.content, post.approved,post.processed, post.id, post.createdAt, post.updatedAt);
//
//	fun Post.fromPostDto(post: PostDto) =
//		Post("post.title", "post.content", post.approved);
}

